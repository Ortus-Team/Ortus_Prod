const React = require("react");
const ReactDOM = require("react-dom");

/*
*/

export class RelatedContent extends React.Component {
  render() {
    return (
      <div id={styles.thing} />
    );
  }
}
